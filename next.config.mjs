/** 
 * @type {import('next').NextConfig} 
 * 
 * Este archivo es la configuración principal de Next.js.
 * Aquí se pueden definir variables de entorno públicas, optimizaciones de imágenes,
 * redirecciones y configuración para el despliegue en Vercel.
 */
const nextConfig = {
  // reactStrictMode ayuda a identificar errores comunes en React durante el desarrollo
  reactStrictMode: true,
};

export default nextConfig;
